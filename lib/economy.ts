// Shekel reward amounts — shared by the store and any container that awards them,
// so the two numbers only ever live in one place.
export const SHEKELS_PER_VERSE_COMPLETED = 5;
export const SHEKELS_PER_VERSE_REVIEWED = 10;
export const SHEKELS_PER_BOSS_BATTLE = 200;
